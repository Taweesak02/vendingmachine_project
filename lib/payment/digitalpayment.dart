import 'package:flutter/material.dart';

class DigitalPayment extends StatelessWidget {
  const DigitalPayment({super.key});

  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text("ชำระด้วยการสแกนจ่าย")),
    );
  }
}
